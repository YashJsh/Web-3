/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { CODAMA_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INCREMENT_COUNTER_DISCRIMINATOR = new Uint8Array([
  16, 125, 2, 171, 73, 24, 207, 229,
]);

export function getIncrementCounterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INCREMENT_COUNTER_DISCRIMINATOR,
  );
}

export type IncrementCounterInstruction<
  TProgram extends string = typeof CODAMA_PROGRAM_ADDRESS,
  TAccountCounter extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCounter extends string
        ? WritableAccount<TAccountCounter>
        : TAccountCounter,
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      ...TRemainingAccounts,
    ]
  >;

export type IncrementCounterInstructionData = {
  discriminator: ReadonlyUint8Array;
  input: number;
};

export type IncrementCounterInstructionDataArgs = { input: number };

export function getIncrementCounterInstructionDataEncoder(): FixedSizeEncoder<IncrementCounterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["input", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INCREMENT_COUNTER_DISCRIMINATOR }),
  );
}

export function getIncrementCounterInstructionDataDecoder(): FixedSizeDecoder<IncrementCounterInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["input", getU32Decoder()],
  ]);
}

export function getIncrementCounterInstructionDataCodec(): FixedSizeCodec<
  IncrementCounterInstructionDataArgs,
  IncrementCounterInstructionData
> {
  return combineCodec(
    getIncrementCounterInstructionDataEncoder(),
    getIncrementCounterInstructionDataDecoder(),
  );
}

export type IncrementCounterAsyncInput<
  TAccountCounter extends string = string,
  TAccountPayer extends string = string,
> = {
  counter?: Address<TAccountCounter>;
  payer: TransactionSigner<TAccountPayer>;
  input: IncrementCounterInstructionDataArgs["input"];
};

export async function getIncrementCounterInstructionAsync<
  TAccountCounter extends string,
  TAccountPayer extends string,
  TProgramAddress extends Address = typeof CODAMA_PROGRAM_ADDRESS,
>(
  input: IncrementCounterAsyncInput<TAccountCounter, TAccountPayer>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  IncrementCounterInstruction<TProgramAddress, TAccountCounter, TAccountPayer>
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODAMA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    counter: { value: input.counter ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.counter.value) {
    accounts.counter.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([99, 111, 117, 110, 116, 101, 114]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.payer.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.counter),
      getAccountMeta(accounts.payer),
    ],
    data: getIncrementCounterInstructionDataEncoder().encode(
      args as IncrementCounterInstructionDataArgs,
    ),
    programAddress,
  } as IncrementCounterInstruction<
    TProgramAddress,
    TAccountCounter,
    TAccountPayer
  >);
}

export type IncrementCounterInput<
  TAccountCounter extends string = string,
  TAccountPayer extends string = string,
> = {
  counter: Address<TAccountCounter>;
  payer: TransactionSigner<TAccountPayer>;
  input: IncrementCounterInstructionDataArgs["input"];
};

export function getIncrementCounterInstruction<
  TAccountCounter extends string,
  TAccountPayer extends string,
  TProgramAddress extends Address = typeof CODAMA_PROGRAM_ADDRESS,
>(
  input: IncrementCounterInput<TAccountCounter, TAccountPayer>,
  config?: { programAddress?: TProgramAddress },
): IncrementCounterInstruction<
  TProgramAddress,
  TAccountCounter,
  TAccountPayer
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODAMA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    counter: { value: input.counter ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.counter),
      getAccountMeta(accounts.payer),
    ],
    data: getIncrementCounterInstructionDataEncoder().encode(
      args as IncrementCounterInstructionDataArgs,
    ),
    programAddress,
  } as IncrementCounterInstruction<
    TProgramAddress,
    TAccountCounter,
    TAccountPayer
  >);
}

export type ParsedIncrementCounterInstruction<
  TProgram extends string = typeof CODAMA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    counter: TAccountMetas[0];
    payer: TAccountMetas[1];
  };
  data: IncrementCounterInstructionData;
};

export function parseIncrementCounterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedIncrementCounterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { counter: getNextAccount(), payer: getNextAccount() },
    data: getIncrementCounterInstructionDataDecoder().decode(instruction.data),
  };
}
