[package]
name = "counter_pinoccio"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = "0.10.2"
pinocchio-system = "0.5.0"
pinocchio-log = "0.5"
borsh = { version = "1.6.0", features = ["derive"] }